[project]
name = "matching-engine"
version = "0.0.1"
description = "公地引擎 · 匹配引擎模块 — 供需匹配（阶段1 首个 PoC）"
requires-python = ">=3.11"
license = { text = "AGPL-3.0-only" }

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["matching_engine"]
