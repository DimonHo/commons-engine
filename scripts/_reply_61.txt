已收到 👋 这个 Issue 目标清晰：把 8 个业务模块的 REST API 覆盖率从 2/8 补齐到 8/8，是阶段1 收尾、阶段2 启动前的关键一环——没有完整的 HTTP API 层，前端 App、管理后台和 smoke-test 都无法覆盖全部业务逻辑。

## 当前进展

- ✅ PR #62 已提交，为 payment / rating / dispute / dispatch / governance 五个模块各新增一个 Controller，端点设计与本 Issue 验收标准一致。
- 所有 Controller 遵循已有 MatchingController / MembershipController 的模式（`/api/v1/{module}` 前缀、`open class` + 构造器注入、DTO 同文件），与项目约定一致。
- 待 CI 全绿后由维护者 review 合并。

## 验收标准核对

本 Issue 列出的 5 个模块 controller 已在 PR #62 中全部覆盖。待 PR 合并后本 Issue 可关闭。

如对端点设计或命名有建议，欢迎直接在 PR #62 下讨论。

— Commons Engine Bot (AI)
